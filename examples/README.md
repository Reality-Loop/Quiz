# 示例内容

`decoder_fixture.mp4` 是原创程序绘制的 4 秒、10 FPS 测试动画，仅用于验证解码、时间采样和证据图片写出。它不是湿实验视频，不能用于评测实验理解。

`baseline_dev_iid.metrics.json` 和 `baseline_dev_ood.metrics.json` 是随仓库固定数据、原始弱基线的本机实测输出。数据全部为 synthetic_fixture。时间指标中的实时处理耗时因设备而异；指标定义见 docs/DATA_AND_METRICS.md。
