"""Replace this working starter with your causal method. See README and docs/TASK.md."""
from wetlab_challenge.baseline import BaselineAgent


class Agent(BaselineAgent):
    """The starter is runnable but is NOT a completed candidate submission.

    Implement reset(context) and update(observation); never access labels,
    episode schedules, future observations, or the fixture generator at inference.
    """
    pass
